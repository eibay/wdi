!!!
%html{:lang => "en"}
	%head
		%meta{:charset => "utf-8"} 
		%title Random Rosencrantzers
	%body
		%header
			%h1 Random Rosencrantzers
	
		%section 
			%h2 All Random Rosencrantzers

			%ul 
				- users.each do |user|
					%li
						%img{:src => user["hipchat_avatar"]}
						%a{:href => "/user/#{user["user_id"]}"}= "#{user["first_name"]} #{user["last_name"]}"

		%section
			%h2 Make a New Random Rosencrantzer 
			%form{:action => "/user", :method => "POST"}
				%button Do It