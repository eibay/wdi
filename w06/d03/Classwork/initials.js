// Define a function initials that takes a person's name as input and returns their initials.
// Use loops for this instead of an enumerator like map.
// It should worked both with and without a middle name being provided.

initials('Neel Jayendra Patel');
// // NJP
var args = process.argv;

var init_result = [];
var names_result = args[2]; 

function initials(fullname) {
// first split the name on space
	var names_split = names_result(split[" "]);
	console.log(names_split)
// loop thru array of names while < array.length
// take each first char and return
	while () {





	}:


};