<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Dog Breeds</title>
</head>
<body>
	<h1>Rosencrantzzz Animal Shelter</h1>
	
	<label>Add a new breed:</label>
	<form action="/breeds/add" method="POST">
		<input type="text" name="name" placeholder="Breed">
		<button>Submit</button>
	</form>
	
	<ul>
		<% breeds.each do |breed| %>
		<li> 
			<% image = Image.find_by(breed_id: breed.id) %>
			<h2> <%= breed.name %> </h2>
			<img src="<%= image.url %>">
			<form action="/breeds/<%= breed.id %>/edit" method="GET">
				<button >Edit</button> 
			</form>
			
			<form action="/breeds/<%= breed.id %>" method="POST">
				<input type="hidden" name="_method" value="DELETE">
				<button >Delete</button> 
			</form> 
		</li>
		<% end %>
	</ul>

</body>
</html>