<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Registration of New Patients</title>
</head>
<body>
<h1> Find Patient by condition</h1>

	<% patients.each do |patient| %>
	<%if patients["condition"] == params[:condition] %>
	<%= patient = patient["first"] + (", ") + patient["last"]+ (", ") + patient ["condition"] %>
	<% puts patient %>
	<% end %>
	<% end %>
  <a href = "/condition"> Find another patient</a>

</body>
</html>