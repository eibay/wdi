<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Patient Search Results</title>
</head>
<body>
	<h1>Seattle Grace Hospital: Patient Search Results</h1>

<h3>Patients named with <%= first %></h3>
<ol>
	<% patient_info.each do |search| %>
      	<% if  search["first"].include?(first) %> 
      	<li><%= search["first"] %> 
      	<%= search["last"] %>
      	was admitted on <%= search["admittance"] %>. Condition: 
      	<%= search["condition"].capitalize %></li>
      	<% end %>
      <% end %>
</ol>    
	<p><a href="/">View Patient Registry</a></p>
	<p><a href="/add">Add a New Patient</a></p>
	<p><a href="/search">Search Again</a></p>
</body>
</html>