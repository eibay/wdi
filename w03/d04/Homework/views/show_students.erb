<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" type="text/css" href="/styles.css">

  <title>Hogwarts Registrar</title>
</head>
<body>
  <div id="wrapper">
    <h2> Registered Hogwarts Students </h2>
    <hr />

    <% students.each do |student| %>
    <p><%= "#{student['first']} #{student['last']}" %><br />
    <%= "Age: #{student['age']}" %><br />
    <%= "Favorite Spell: #{student['spell']}" %><br />
    <%= "Familiar? #{student['familiar']}" %></p>
    <% end %>

  <a href="/">Home</a>
  </div>
</body>
</html>
