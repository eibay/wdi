// Create a function that returns a string with a joke.

function tellJoke(str) {

return str
};

var joke = "Q: Why did the monkey fall out of the tree? A: Because it was dead."
console.log(tellJoke(joke));


function tellJoke() {
var joke = "Q: Why did the monkey fall out of the tree? A: Because it was dead."

return joke
};

// var joke = "Q: Why did the monkey fall out of the tree? A: Because it was dead."
console.log(tellJoke());