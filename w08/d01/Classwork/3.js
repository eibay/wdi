function stringInfo(str){
	return hash = {
		"length" => str.length,
		"original" => str,
		"splitBySlash" => str("/").split
	}

	
}

// var obj = {}
// obj["name"] = "sean"
// obj ["name"]
// obj.name