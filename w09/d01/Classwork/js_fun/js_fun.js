/*Write a function that takes an array of integers and an integer as arguments. The function should 
return true if the given integer appears in the array, and false if it does not. */

function inArray([1..9], integer){
	if integer = [i][1..9],
		return true
} else if 
        return false