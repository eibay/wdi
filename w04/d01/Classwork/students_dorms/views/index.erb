<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>List</title>
</head>
<body>
	<a href = "/students/new">Add New Student</a>
  <ul>
    <% students.each do |student| %>
      <li><%= student["first"] %> <%= student["last"] %> - <%= student["email"] %></li>
    <% end %>

  <br>

    <a href = "/dorms/new">Add your Dorm Name</a>

    <%  dorms.each do |dorm| %>
      <li><%= dorm["name"] %> <%= dorm["capacity"] %></li>
      <% end %>
  </ul>
</body>
</html>