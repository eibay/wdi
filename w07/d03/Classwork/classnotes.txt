1. we have callbacks in js so that when an event happens, a function can be fired off for processing and the rest of the program can carry on.

2. the browser or program seems to call a callback when it runs across code in the browser
